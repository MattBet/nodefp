extends layout

block body
    h1= article.title
    .text-muted Written by #{author}
    p= article.body
    hr
    if user
        if user.id === article.author
            a.btn.btn-success(href='/articles/edit/'+article._id) Edit
            a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete